# Data

The first baseline keeps inputs parameterized in code to stay deterministic and easy to reproduce.
This directory is reserved for future SDF snapshots, mesh fixtures, or paper-specific datasets.
